# Local HTML delivery

Turn `template.html` into a complete standalone document: add the doctype, `html`, `head` and `body`; put the title, UTF-8 charset, viewport metadata and styles in the head, and the page content and scripts in the body. Keep CSS and JavaScript inline so the file opens without a server or build step.

Return a clickable link to the absolute file path. On the user's own machine, open the file using the available browser or OS capability.
